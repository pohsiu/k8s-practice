apiVersion: v1
kind: Service
metadata:
  name: express-app-{{PR_BRANCH}}
  labels:
    app: express-app
    branch: {{PR_BRANCH}}
spec:
  type: ClusterIP
  selector:
    app: express-app
    branch: {{PR_BRANCH}}
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP

